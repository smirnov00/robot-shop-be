swagger: "2.0"
info:
  description: "This is the robot-shop API documentation"
  version: "1.0.0"
  title: "roboT-Shop"
host: "6acgl8dup9.execute-api.eu-west-1.amazonaws.com"
basePath: "/dev"
schemes:
- "https"
paths:
  /products:
    get:
      tags:
        - "products"
      produces:
        - "application/json"
      responses:
        "200":
          description: "List of products"
          schema:
            type: array
            items:
              $ref: "#/definitions/Product"
    post:
      tags:
        - "products"
      parameters:
        - in: body
          name: body
          required: true
          schema: 
            $ref: "#/definitions/Product"
      responses:
        "204":
          description: "Product has been created"
        "400":
          description: "Invalid payload"
  /products/{productId}:
    get:
      tags:
        - "products"
      produces:
        - "application/json"
      parameters:
        - in: "path"
          name: "productId"
          required: true
          type: string
      responses:
        "200":
          description: "Product"
          schema:
            type: object
            properties:
              product:
                $ref: "#/definitions/Product"
        "404":
          description: "Product not found"
  /import:
    get:
      tags:
        - "import"
      parameters:
        - in: query
          name: name
          required: true
          schema: 
            type: string
      produces:
        - "application/json"
      responses:
        "200":
          description: "signed url"
          schema:
            type: object
            properties:
              url:
                type: string
definitions:
  Product:
    type: "object"
    properties:
      id:
        type: string
      title:
        type: string
      description:
        type: string
      count:
        type: integer
      price:
        type: number