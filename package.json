{
  "type": "module",
  "scripts": {
    "test": "jest",
    "generate-mock-products:csv": "node ./utils/mock-data/generate.js --format=csv",
    "generate-mock-products:json": "node ./utils/mock-data/generate.js --format=json"
  },
  "devDependencies": {
    "@babel/core": "^7.15.5",
    "@babel/preset-env": "^7.15.6",
    "@types/jest": "^27.0.1",
    "babel-jest": "^27.2.0",
    "jest": "^27.2.0"
  }
}
